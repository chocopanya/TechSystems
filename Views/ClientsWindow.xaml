<Window x:Class="TechSystems.Views.ClientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление клиентами" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Управление клиентами" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Панель поиска -->
        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Поиск клиентов:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtSearch" Height="30" 
                         TextChanged="TxtSearch_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
        </Border>

        <!-- Таблица клиентов -->
        <DataGrid Grid.Row="2" x:Name="dgClients" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Компания" Binding="{Binding CompanyName}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="120"/>
                <DataGridTextColumn Header="Роль" Width="120">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0}">
                            <Binding Path="RoleId"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RoleId}" Value="1">
                                    <Setter Property="Text" Value="Администратор"/>
                                    <Setter Property="Foreground" Value="Purple"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RoleId}" Value="2">
                                    <Setter Property="Text" Value="Менеджер"/>
                                    <Setter Property="Foreground" Value="Blue"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RoleId}" Value="3">
                                    <Setter Property="Text" Value="Клиент"/>
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Просмотреть заявки" Width="140" Height="35" Margin="5"
                    Background="#2196F3" Foreground="White" FontWeight="Bold"
                    Click="BtnViewApplications_Click"/>
            <Button Content="Просмотреть платежи" Width="140" Height="35" Margin="5"
                    Background="#FF9800" Foreground="White" FontWeight="Bold"
                    Click="BtnViewPayments_Click"/>
            <Button Content="Обновить" Width="100" Height="35" Margin="5"
                    Click="BtnRefresh_Click"/>
            <Button Content="Закрыть" Width="100" Height="35" Margin="5"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>