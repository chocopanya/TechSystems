<Window x:Class="TechSystems.Views.ClientApplicationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заявки клиента" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" x:Name="txtTitle" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Таблица заявок -->
        <DataGrid Grid.Row="1" x:Name="dgApplications" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Тариф" Binding="{Binding Tariff.Name}" Width="180"/>
                <DataGridTextColumn Header="Тип услуги" 
                                    Binding="{Binding Tariff.ServiceType.Name}" 
                                    Width="120"/>
                <DataGridTextColumn Header="Дата заявки" 
                                    Binding="{Binding ApplicationDate, StringFormat='dd.MM.yyyy'}" 
                                    Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Новая">
                                    <Setter Property="Foreground" Value="Blue"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="В обработке">
                                    <Setter Property="Foreground" Value="Orange"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Подтверждена">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Отменена">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Завершена">
                                    <Setter Property="Foreground" Value="Purple"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Лицензии" 
                                    Binding="{Binding LicenseCount}" 
                                    Width="80"/>
                <DataGridTextColumn Header="Стоимость" 
                                    Binding="{Binding TotalCost, StringFormat='{}{0:N2} руб.'}" 
                                    Width="120"/>
                <DataGridTextColumn Header="Комментарий" 
                                    Binding="{Binding Comment}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель кнопок -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Подробнее" Width="100" Height="35" Margin="5"
                    Click="BtnDetails_Click"/>
            <Button Content="Обновить" Width="100" Height="35" Margin="5"
                    Click="BtnRefresh_Click"/>
            <Button Content="Закрыть" Width="100" Height="35" Margin="5"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>